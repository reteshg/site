import React from 'react';

const Features = (props) => (
    <span>
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        </span>
);

export default Features;