import React from 'react';

const Features = (props) => (
    <p>
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        </p>
);

export default Features;