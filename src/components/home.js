import React from 'react';

const Home = (props) => (
   

    <span>
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        </span>      
);

export default Home;