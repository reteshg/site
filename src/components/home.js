import React from 'react';

const Home = (props) => (
    <p>
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        <h1>{props.title}</h1>    
        </p>
);

export default Home;